# 242. Valid Anagram

## Difficulty

Easy

## Topics

- String
- Hash Table
- Sorting
- Character Frequency

## Problem

Given two strings `s` and `t`, return `True` if `t` is an anagram of `s`. Otherwise, return `False`.

An anagram contains exactly the same characters with exactly the same frequencies, possibly arranged in a different order.

## Examples

### Example 1

```text
Input: s = "anagram", t = "nagaram"
Output: true
```

### Example 2

```text
Input: s = "rat", t = "car"
Output: false
```

## Constraints

```text
1 <= s.length, t.length <= 50,000
s and t contain only lowercase English letters
```

## Follow-up

How would the algorithm change if the strings could contain Unicode characters?

## LeetCode

[242. Valid Anagram](https://leetcode.com/problems/valid-anagram/)
